syntax = "proto2";

import "wcif/venue.proto";

package cubeconductor.api.wcif;

message Schedule {
  optional string start_date = 1;
  optional int32 number_of_days = 2;
  repeated Venue venues = 3;
}
